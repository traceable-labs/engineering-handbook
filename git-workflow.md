# Git Workflow

We follow a feature-branch workflow:

- `main` branch is protected and always deployable.
- Feature branches should follow naming scheme: `feature/<short-desc>` or `fix/<short-desc>`.
- All pull requests require at least 1 peer approval before merge.
- Keep commits atomic and messages descriptive.
